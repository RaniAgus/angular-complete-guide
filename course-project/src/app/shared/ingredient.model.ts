export class Ingredient {
  // public name: string;
  // public amount: number;

  constructor(public name: string, public amount: number) { // Esto nos provee las propiedades automáticamente
    // this.name = name;
    // this.amount = amount;
  }
}
